
csweb
  mv db logic (name suffix + db type) to some class ?+ kvdb (prefsuffix)
  back to sqlite, put directory on path
  
  have a emitflag for turnonall thats in nonrel builds

  xam ssl
  see about improving xam ssl or use bouncy
  add imap get hub working
    
  why is stay logged in not working again
  
xamios
  xamsqlite

try on phone, then android package on bitbucket

other cmd options, like play sound file, play sound on loop
  refresh for action links (add in hub) calls the thing that refreshes dev and other
  the do for mp3cycle android ver
  hold state, start and stop
  
sitename not deleted after internet listen removed, staying in wc

up/download pics automatically - imap

auto-update

update for dependencies (script...)

generic redo update and fwd for changes like imap, service, cloud setup
  call in all those cases

retest setup

feedback when imap setup / pass is wrong
  have be part of update/links update
  have links update be a separate call after login update
  have a refresh option in the links list
  detect webcams (link at least) does the wrong thing, have it do the update

outside db key for en/decrypt values in config, in sec store on mobile

cleanup expired sessions and old oncetokens
expiration for once login (deletem on cleanup)
old session cleanup (do on bad ip cleanup)

bring back createadmin

can change expiration during session

default session time on regular login from config

need retry script for failure to launch during install

====

multi process embedded database for java / android

automation for install inst on internet listen / hosting

rm old junk from cam

android ver (full)

droid
  no dev name - get from phone
  
auto dl pics

sharing urls for other apps

enter key support for submit wherever it makes sense
  device login, device name

options for problems in wiki
make sure contact method works to ping me (?email fwd)

optional encrypte/decrypt pass for payloads in imap

should do reference counting for certificate print in/out

add sha256 thumbprint too (for viewing)

switch to sha256 for verification

backup stuff (pic, etc) to pi from clients

sending email to update folks on change
  look in inbox, by subject, up to last check time (max date last time - some hours)
  encrypting payload with key for verification, provide during servicelogin, paired with
  deviceid

For embedded stay logged just need to store and retrieve the session from config not
  all the other stuff - see checkin 2017-02-05, leave the 30 min default
  can just always use the embedded thing instead of looking for cookie for embedded req
  test that expiration works
  ?make initial login non-expiring?  Or just really long, like 12-24 hours

zwave etc

iot dev sec (wifi, bt, ?)
  dev ssid, bcast, setup (app)
  set ssid, key, join
  (instructs ssid key) per-dev

mac, win ver (xamarin forms)
  have the notion of a proxy that can wake a box, call it (def by devid)
  cs ws - use the httplistener
    ?move to bouncy castle for cert gen?
    ?or just figure out why it's not liked?


library based dev env
  builds for each thing (at least sep jv and js)
  sep into lib (from base) and rest (iot) 
  (so, 2x2)
  one build script with args to decide mode? move back args to exe by 1
  dynamic dev for plugins (own, added) and ui components to go with (app like cam with
    all the basics)

script to stop everything and start lwui

remote access stuff

pics autosend/get auto-download pics to app with retention time

ping location

on host ssh setup instructs and script (sshd_config change)
  
iot (lights, etc)
homeauto - light
  java wrapper openzw? node + openzw?
homeauto - locks

haproxy for privilaged -> localhost

siteNames ui
real certs (let's encrypt and the other one, pay ones)
  
check for updates - two places (sf and github?)
  size? sha?
auto update (turn off able, on by default)
______________

simple event stuff, alert/notify

support dyn dns options (duck, ?)

=====

upload in progress indicator
  id for upload process
  periodic/timer calls from ui
  
temporary picture access/sharing on network (email link)
  
play sounds from file manager, incl looop n times (wh noise, include by dl?)
mixer settings for output and vol?
record sound snapshot (also after pic from motion)
  
launch ec2 from imap info for no-ssh version (on demand)
______________
  
force deviceid to certain characteristics (in get)

secure tunnel for vnc (encrypted port fwd)
______________

static forward for things like ssh from local port on client
______________

cam periodic pic

persist the last update time, have an update now (refresh) option

instead of omitting unshowable configs put stars and only a delete link
extra security with thumbprint
______________
  
direct hosting option with imap and webmail
______________

all in browser dev sandbox
______________

reverse file order

db pooling kvdb (no clocker) option
  
scheduling 

picture navigation (prior/next)
______________

check and kill motion if api unresponsive

sound 

hc default inst?
  
video

basic link working wincs, monoweb, xamarin
  use mono sqlite instead of firebird
  monoweb with just build module, link, hub

robots

good event development/scripting abilities!!! across devs (home auto, etc)

record sound
play uploaded sound
alarm

also have a rasberry pi connected wakeup option

password sync/ vault
contact sync
bookmark sync
text backup
location tracking/find phone
move cert stuff out of app (to webserver?, or sharable place...)

have a sync adapter/ios background thing that updates on occasion
also working in c# windows imap  
xamarin studio uwp - xamarin forms
ios

imap coordinator as-is for links/entry (hub)
also sends emails (see offerLinkRequest) - list of email addresses which are updated (comma sep)
  put in code (sha with time, nonce, shared secret) also check who email is from and retain that
  on first use call with code and get a token (only allowed once) so later compromise not a problem
  check ssl sig
  used from then on
  hub sends on change, time preiod, demand
  link scans on occasion or demand
______________

______________

duckdns (for fixed port)
totally online option for hub - dreamhost
  host own imap
  no-upnp option ssh tunnel
______________


sharing urls with other apps
  first urls javafx, android, cs (win, ios)
    empty ui (hi, back/forth) all (add extra compile config info for csc)
    db for config all
    imap all
    links all (then open in native browser all)
    dump no longer really implemented contentGet in browsers

  expose way to let apps get the info (they may choose to), with docs & way to 
  add to app

______________

filemanager/sync - seafile, pydio, owncloud (needs port forward/intents/app changes)
filemanager, remote access

other app support
  desktop - static port forward, shared files
  android - port forward and intents
  ios - sharing (pita, uipasteboard*? url(back and forth)*? extension?)
    url-url example - https://kotikan.com/blog/item/passing-data-between-ios-apps
    receive call url
    paste into pasteboard, or just callback
    callback
    (only do this if last one in pasteboard no workie, or age, or empty)
