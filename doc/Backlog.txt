
^^^^

!!!!

draftii

save imap settings button, close without save button
menu to open close settings
when not set have a message to suggest it

background sync, timed update in ui

sync back/forth imap
  sync up on save
  subj rand val, timestamp, sequence, subject
  background when open
    
separate off webapp rm from draftii

all platforms

out there?
 
jv/lnx, android, 
win
cc mac ios

store imap password well per platform
use background sync / newstand ios android
uglifyjs

out there fs

help doc

have remove password in the protect page
have a clear remembered passwords button
put password status with Compose (protected by / not protected by) (Password is set/is not set)

tests

optional global password to set or change a password on a doc (and can be used as the pass, have a button for that)

keep track of state in config - in list (and filter) or what doc open
  ?sync it?

option to change all docs with one password to another
  ?backups too - think no
  
on device backups
   view like drafts with a date
   just a restorebutton
   happens on every save/delete
    or better - happens on time passing?
    


!!!!


  checkbox for glob search
  
single level category
  pure side menu is promising
  remember the last one/the one from the search

optional category password
  category decrypts all at once
  subjects then available

have a way to do oversize saves for large drafts, also attachments, have a load save for them
  spill to disk, for both attachment and large ones

better editor

^^^^
  
====

support without request signature too

sep out pagetoken to diff plugin

jscore ios for logic
  sqlite, web client

make sure things work without hub
    put a couple small ones together to be sure they work
  plugins can be no auth / ahead of auth
  get rid of duplicate auth, file stuff in hub
  move devicename stuff to hub
  finish hub configManager cleanup
  
session / session length to separate plugin ? (checkrenew) (tracking manager)?
    have a background for it, clear tracking manager there
  
integration tests

have a webappstart, pull webapp part out, or do with reflection

get rid of *Start classes

cross site checks to separate plugin?
  
update gui
plugins with own html, share styles, etc via convention

dynamic/codable/sandbox plugin for own code
  is in plugin path, work on request (whiteboard)

plugins have startup, shutdown, handlers
  ?background more embedded in app / timer?

setup profiles for auth web, no auth web, browser apps, etc
  
more tests

port to js, mac, win, android
  ssh/putty win, ssh mac/linux, jsch android
  miniupnpc everywhere
  no bridge ios, just hub

libraries

tease out some hub plugin stuff (updates, etc)

plugin building and packaging

could use wireless connector url to bring up connection (no app needed)

just do an auto detect cams if not done before

pics update svc when taken (intent or motion)
  first intent, then motion
  motion is with configured token
  log it
push into sync
  
pull if configured for sync
android notify
android background

db pool
  use db copy()
  how many to keep
  return to reuse
  return failed
  (above cooperative)
  
===

android auto-notify on motion pic
  background imap update
  get links
  notify

try on phone

android package on bitbucket

play/appstore - export

auto-dl (wakeup occasionally, get changes)
  keep pic list
  compare against what have
  put in dir named for devicename
  shows up in own pic dir on phone

headless setup pi

remote install over ssh for hosted

mini plugins for white noise, etc

outside db key for en/decrypt values in config, in sec store on mobile

cleanup expired sessions and old oncetokens
expiration for once login (deletem on cleanup)
old session cleanup (do on bad ip cleanup)

need retry script for failure to launch during install

====

flutter

upgrade check two places

droid
  no dev name - get from phone

sharing urls for other apps

options for problems in wiki
make sure contact method works to ping me (?email fwd)

should do reference counting for certificate print in/out

add sha256 thumbprint too (for viewing)

switch to sha256 for verification

library based dev env
  builds for each thing (at least sep jv and js)
  sep into lib (from base) and rest (iot) 
  (so, 2x2)
  one build script with args to decide mode? move back args to exe by 1
  dynamic dev for plugins (own, added) and ui components to go with (app like cam with
    all the basics)

ping location

on host ssh setup instructs and script (sshd_config change)

haproxy for privilaged -> localhost

siteNames ui
real certs (let's encrypt and the other one, pay ones)

upload in progress indicator
  id for upload process
  periodic/timer calls from ui
  
temporary picture access/sharing (email link)
  
static forward for things like ssh from local port on client

cam periodic pic

instead of omitting unshowable configs put stars and only a delete link

all in browser dev sandbox

check and kill motion if api unresponsive

sound 

alarm

also have a rasberry pi connected wakeup option
